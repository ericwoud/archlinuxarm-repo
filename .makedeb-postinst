###### Start of inserted file #####

  after_install_part2 "$@"
} # We are included within function after_install(), but we need main code

if [ "$1" = triggered ]; then
    after_upgrade "${2}"
    exit 0
fi

after_install_part2() { # Continue inside a function again

###### End of inserted file #####
