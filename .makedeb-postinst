###### Start of inserted file #####

  after_upgrade "${2}"
} # We are included within function after_install(), but we need main code

if [ "$1" = triggered ]; then
  pb="linux-${2##*.}-git"
  /usr/sbin/depmod "${pb}"
  exit 0
fi

dummy_function() { # Continue inside a function again
    :
###### End of inserted file #####
