post_upgrade() {
  sudo systemctl --force --no-pager disable ssh-fix-reboot.service
  sudo systemctl --force --no-pager enable ssh-fix-reboot.service
}

post_install() {
  post_upgrade
}

pre_remove() {
  sudo systemctl --force --no-pager disable ssh-fix-reboot.service
}
