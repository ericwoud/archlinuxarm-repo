post_upgrade() {
  sudo systemctl --force --no-pager reenable ssh-fix-reboot.service
}

post_install() {
  post_upgrade
}

pre_remove() {
  sudo systemctl --force --no-pager disable ssh-fix-reboot.service
}
