#!/bin/bash

unamer="${1}"
if [[ "${2##*.}" == "itb" ]]; then
  linuxpkg="linux-image-${unamer}" # from altered mkdebian
else
  linuxpkg="${2#*Image-}"
fi

bpir-toolbox --remove-dtb --for-linux="${linuxpkg}"

bpir-toolbox --clean-boot --for-linux="${linuxpkg}"

rm -vf /lib/modules/${unamer}/pkgbase

exit 0
